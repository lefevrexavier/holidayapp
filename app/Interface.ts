
  export interface Holiday {
    employee: string;
    start: Date;
    end: Date;
    status: string;
    category: string;
  }

  export interface status {
  }

  export interface categoryHoliday {
  }